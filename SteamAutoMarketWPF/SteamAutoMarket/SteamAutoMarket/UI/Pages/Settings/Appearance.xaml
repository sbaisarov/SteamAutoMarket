<UserControl x:Class="SteamAutoMarket.UI.Pages.Settings.Appearance"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:settings="clr-namespace:SteamAutoMarket.UI.Pages.Settings"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance settings:Appearance}">
    <Grid>
        <ScrollViewer>
            <StackPanel x:Name="Panel" MinWidth="150">
                <TextBlock Text="APPEARANCE" Style="{StaticResource Heading2}" Margin="0,0,0,8" />

                <ListBox ItemsSource="{Binding AccentColors}"
                         SelectedItem="{Binding SelectedAccentColor, Mode=TwoWay}"
                         Margin="0,0,0,16"
                         ToolTip="Global theme secondary color style">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel MaxWidth="{Binding ActualWidth, ElementName=Panel}" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Rectangle Width="40" Height="40" Margin="2">
                                <Rectangle.Fill>
                                    <SolidColorBrush x:Name="Brush" Color="{Binding}" />
                                </Rectangle.Fill>
                            </Rectangle>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Grid HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition MinWidth="120" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Theme:" VerticalAlignment="Center" Margin="0,0,16,12" ToolTip="Global visual theme style" />
                    <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Themes}"
                              SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" DisplayMemberPath="DisplayName"
                              VerticalAlignment="Center" Margin="0,0,0,12" />

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="Working chart (max)"
                               VerticalAlignment="Center"
                               Margin="0,0,16,12"
                               ToolTip="Dots count on working process chart to optimize dots in more compact format"/>

                    <xctk:IntegerUpDown Grid.Row="1"
                                        TextAlignment="Left"
                                        Style="{StaticResource UpDown}"
                                        Grid.Column="1"
                                        Value="{Binding WorkingChartMaxCount, Mode=TwoWay}"
                                        VerticalAlignment="Center"
                                        Margin="0,0,0,12"
                                        Minimum="1"
                                        Maximum="9999999" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>